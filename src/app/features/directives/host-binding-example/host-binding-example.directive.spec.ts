import { HostBindingExampleDirective } from './host-binding-example.directive';

describe('HostBindingExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new HostBindingExampleDirective();
    expect(directive).toBeTruthy();
  });
});
